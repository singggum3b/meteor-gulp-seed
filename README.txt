

* Development with mockup data
	npm start

* Development with backend data
	npm run serve

* Build assets for backend
	(do it after pushing code)
	npm run build

* Push changes to develop
	npm run push

* Pull from develop
	npm run pull

* Pull latest code then build and push new changes to develop
	(do it after your commit)
	npm run push-all
